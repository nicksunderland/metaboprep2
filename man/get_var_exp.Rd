% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_metabolites.R
\name{get_var_exp}
\alias{get_var_exp}
\title{Retrieve Variance Explained by PCs}
\usage{
get_var_exp(metabolites, layer, as_df = FALSE)
}
\arguments{
\item{metabolites}{A \code{Metabolites} object containing the principal component data.}

\item{layer}{A character string specifying the data layer from which to extract the PCs.}

\item{as_df}{Logical. Whether to return the result as a \code{data.frame}. Defaults to \code{FALSE}, returning a matrix.}
}
\value{
A matrix or \code{data.frame} (if \code{as_df = TRUE}) containing the principal components.
}
\description{
Extracts the variance explained by each principal components (PCs) from a \code{Metabolites} object
for a specified data \code{layer}, with optional exclusions and subsetting.
}
\examples{
file <- system.file("extdata", "metabolon_v1_example.xlsx", package = "metaboprep2")
metabolites <- Metabolites(project_name = "MYPROJECT",
                           format       = "metabolon_v1",
                           filepath     = file)
metabolites <- import_data(metabolites)
metabolites <- metabolite_qc(metabolites, source="raw")
get_var_exp(metabolites, layer = "post_qc")

}
